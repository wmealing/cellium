#{id => something,
  type => widget,
  expand => true,
  widget_type => frame,
  text => <<"FRAME AS ANYTHING">>,
  padding => #{left => 0,
               right => 0,
               top => 0,
               bottom => 0}}.
